version: 2.1
  jobs:
    build:
      docker: 
        - image: circleci/node:10.x
      steps:
        - checkout
        - run: npm build
        # - run: npm updateExampleHTMLs
        - run: npm test
